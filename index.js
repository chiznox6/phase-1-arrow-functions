// Declare a function using a function expression
const divide = function() {
  return 2000 / 100;
};

// Declare a function using arrow syntax
const square = x => x * x;

// Declare a function using arrow syntax with two parameters
const add = (a, b) => a + b;
